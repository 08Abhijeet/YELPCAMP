<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>title</title>
  </head>
  <body>
    <h1> Edit the campgrounds here</h1>
    <form action="/campgrounds/<%=campgrounds._id %>?_method=PUT"  method="post">
      <div>
        <label> Enter the title:  </label>
        <input type="text" id="title" name="campgrounds[title]" value="<%= campgrounds.title %>" />
    </div>
      <div>
        <label> Enter the location:  </label>
        <input type="text" id="location" name="campgrounds[location]" value="<%= campgrounds.location%>" />
      </div>
      <br>
    <button> Update campground</button>
    <br>
    <a href="/campgrounds/<% campgrounds._id %>"> Back to campgrounds</a>
    </form>
  </body>
</html>
